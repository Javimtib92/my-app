const aliases = (prefix = `src`) => ({
  '@ui': `${prefix}/ui`,
  '@config': `${prefix}/config`,
  '@api': `${prefix}/api`,
  '@utils': `${prefix}/utils`,
  '@pages': `${prefix}/pages`,
  '@components': `${prefix}/components`,
})

module.exports = aliases
